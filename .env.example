# Segredo para assinar os JWTs (gere com: openssl rand -base64 48)
JWT_SECRET=troque-por-um-segredo-longo-e-aleatorio

# Deixe vazio para usar o PGlite local (./.pglite).
# DATABASE_URL=postgres://user:senha@host:5432/banco
